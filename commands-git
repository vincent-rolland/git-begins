How to GIT CLONE :

in command line, enter the following command :

$ git clone [https://github.com/your-name/yourrepo.git]


How to PUSH on GITHub: 

$ git add [file.txt] 
$ git commit -m "Todays Modification"
$ git push -u origin main

How to Pull on GITHub :

$ git pull

How to CREATE a branch :

$ git branch [branch_name]

How to switch from branch to branch :

If it already exists :

$ git checkout [branch_name]

switching to and creating a new one :

$ git checkout -b [new_branch_name]
